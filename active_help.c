#include<graphics.h>                      //绘图库头文件，绘图语句需要
#include<conio.h>                           //在控制台输入输出文件
#include <stdio.h>
#include <stdlib.h> 

int draw(){
    int Row = 10;//5*2
    int Colum = 10;
    int width = 80;
    int height = 40;
    int count = 0;
    initgraph(1000, 800);	 //初始化一个640*480的窗口，
    setbkcolor(WHITE);
    cleardevice();

    return 0;
}